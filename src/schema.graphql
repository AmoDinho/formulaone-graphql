# import Driver from "./generated/prisma.graphql"

type Query {
    info: String!
    feed: [Driver]!
    
}

type Mutation {
    driver(name: String!, team: String!, points: Int!): Driver!
    signup(email: String!, password:String!,name:String!): AuthPayload
    login(email: String!, password: String!): AuthPayload
}

type User {
    id: ID!
    name:String!
    email: String!
    drivers: [Driver!]!
}
