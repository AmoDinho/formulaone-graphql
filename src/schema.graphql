# import Driver, DriverSubscriptionPayload from "./generated/prisma.graphql"

type Query {
    info: String!
    feed: [Driver]!
    
}

type Mutation {
    driver(name: String!, team: String!, points: Int!, pictureURL: String!, podiums: Int!, championshipWins:Int!): Driver!
    signup(email: String!, password:String!,name:String!): AuthPayload
    login(email: String!, password: String!): AuthPayload
  
}

type AuthPayload{
    token: String
    user: User
}

type User {
    id: ID!
    name:String!
    email: String!
    drivers: [Driver!]!
}


type Subscription {
    newDriver: DriverSubscriptionPayload
}

