type Query {
    info: String!
    feed: [Driver]!
}

type Mutation {
    driver(name: String!, team: String!, points: Int!): Driver!
}

type Driver {
    id: ID!
    name: String!
    team: String!
    points: Int!
}